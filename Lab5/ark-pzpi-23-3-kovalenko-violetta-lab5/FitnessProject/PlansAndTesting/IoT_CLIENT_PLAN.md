# Детальний план розробки IoT клієнта з GUI (імітація Apple Watch)

## 🎯 Що буде на виході

**Python додаток з GUI** (імітація Apple Watch), який:
1. **Симулює датчики** → відправляє телеметрію на сервер
2. **Відображає дані** → отримує дієти, рекомендації, статистику з сервера
3. **Повна інтеграція** з .NET сервером через REST API

**Архітектура:**
```
┌─────────────────────────────────────┐
│   Python IoT Client (GUI App)      │
│   ┌─────────────┬──────────────┐   │
│   │  Симулятор  │   Відображення│   │
│   │  датчиків   │   даних       │   │
│   └──────┬──────┴───────┬───────┘   │
└──────────┼──────────────┼───────────┘
           │              │
           ▼              ▼
    ┌──────────────┐ ┌──────────────┐
    │ POST /api/   │ │ GET /api/    │
    │ telemetry/   │ │ dailydietplans│
    │ receive      │ │ statistics   │
    └──────────────┘ └──────────────┘
           │              │
           └──────┬───────┘
                  ▼
         ┌─────────────────┐
         │  .NET Server    │
         │  (localhost:5006)│
         └─────────────────┘
```

---

## ⏱️ Розподіл часу (8 годин)

### **Година 1: Підготовка та структура проекту (60 хв)**

**Завдання:**
1. Створити структуру папок проекту
2. Налаштувати Python середовище (venv)
3. Створити `requirements.txt` з бібліотеками
4. Проаналізувати API сервера (які ендпоінти доступні)
5. Створити базові файли модулів (порожні класи)

**Структура проекту:**
```
iot-client/
├── config/
│   ├── config.json
│   └── config.default.json
├── src/
│   ├── main.py
│   ├── api_client.py
│   ├── sensor_simulator.py
│   ├── watch_gui.py
│   ├── config_manager.py
│   └── logger.py
├── diagrams/
│   ├── use_case_diagram.puml
│   └── activity_diagram.puml
├── requirements.txt
├── README.md
└── .gitignore
```

**Бібліотеки для requirements.txt:**
- `requests==2.31.0` - HTTP клієнт
- `schedule==1.2.0` - Планувальник задач
- `python-dotenv==1.0.0` - Змінні оточення

**Вбудовані модулі Python (не потрібно встановлювати):**
- `tkinter` - GUI фреймворк
- `threading` - багатопоточність
- `json`, `datetime` - стандартні модулі

---

### **Година 2-3: UML діаграми (120 хв)**

#### 2.1. Діаграма прецедентів (Use Case)

**Актори:**
- IoT Device (основний)
- Server (система)
- User (користувач)

**Прецеденти для реалізації:**
1. Налаштування параметрів клієнта
2. Підключення до сервера
3. Зчитування даних з датчиків
4. Надсилання телеметрії на сервер
5. Надсилання даних про сон
6. Надсилання даних про тренування
7. Отримання плану дієти з сервера
8. Отримання рекомендацій з сервера
9. Отримання статистики з сервера
10. Відображення даних в GUI
11. Обробка помилок підключення
12. Оновлення конфігурації
13. Скидання до дефолтних налаштувань

**Інструмент:** PlantUML (онлайн або VS Code extension)

#### 2.2. Діаграма діяльності (Activity)

**Основні процеси:**
1. Ініціалізація клієнта (завантаження конфігурації, створення GUI)
2. Цикл роботи:
   - Зчитування даних з датчиків
   - Відправка на сервер
   - Отримання даних з сервера
   - Оновлення GUI
3. Процес відправки телеметрії (з retry логікою)
4. Процес отримання та відображення даних

**Інструмент:** PlantUML

---

### **Година 4-5: Реалізація бізнес-логіки (120 хв)**

#### 4.1. API Client (`api_client.py`)

**Методи для відправки даних (POST):**
- `send_telemetry()` - відправка телеметрії (пульс, кроки, тиск)
- `send_telemetry_batch()` - батч телеметрії
- `send_sleep_record()` - дані про сон
- `send_training_session()` - дані про тренування

**Методи для отримання даних (GET):**
- `get_daily_diet_plans()` - список планів дієти
- `get_daily_diet_plan(plan_id)` - деталі плану з прийомами їжі
- `get_recommendations()` - рекомендації користувача
- `get_daily_statistics()` - статистика за день
- `get_weekly_statistics()` - статистика за тиждень

**Функціональність:**
- Обробка помилок (try-except)
- Retry логіка (3 спроби з затримкою)
- Логування помилок
- Timeout для запитів (10 секунд)

#### 4.2. Sensor Simulator (`sensor_simulator.py`)

**Методи симуляції:**
- `read_heart_rate()` - пульс (60-100 bpm)
- `read_steps()` - кроки (накопичувальний лічильник)
- `read_blood_pressure()` - тиск (систолічний/діастолічний)
- `generate_sleep_data()` - дані про сон (загальний, глибокий, легкий сон, якість)
- `generate_training_session()` - дані про тренування (тип, інтенсивність, тривалість, калорії)

**Логіка:**
- Використання `random` для генерації реалістичних значень
- Накопичувальні значення (кроки)
- Реалістичні діапазони значень

#### 4.3. Config Manager (`config_manager.py`)

**Функціональність:**
- Завантаження конфігурації з JSON файлу
- Збереження конфігурації
- Скидання до дефолтних значень
- Оновлення окремих налаштувань
- Валідація конфігурації

**Параметри конфігурації:**
- `server.base_url` - URL сервера
- `server.timeout` - таймаут запитів
- `server.retry_attempts` - кількість повторних спроб
- `device.device_id` - ID пристрою
- `device.user_id` - ID користувача
- `sensors.*_interval` - інтервали зчитування датчиків

---

### **Година 6: Реалізація GUI (60 хв)**

#### 6.1. Watch GUI (`watch_gui.py`)

**Структура GUI (Tkinter):**
- Головне вікно (400x600, чорний фон)
- Вкладки (Notebook) для різних екранів:
  1. **Головна** - дашборд (час, пульс, кроки)
  2. **Дієта** - план дієти на день
  3. **Рекомендації** - рекомендації системи
  4. **Статистика** - статистика за день/тиждень
  5. **Датчики** - управління симуляцією

**Компоненти головного екрану:**
- Відображення поточного часу (оновлюється кожну секунду)
- Показ пульсу (з датчиків)
- Показ кроків (з датчиків)
- Кнопка оновлення даних

**Компоненти екрану дієти:**
- Область тексту для відображення плану
- Кнопка завантаження плану з сервера
- Форматування: назва плану, прийоми їжі, калорії, макронутрієнти

**Компоненти екрану рекомендацій:**
- Область тексту для відображення рекомендацій
- Кнопка завантаження з сервера
- Форматування: тип рекомендації, деталі, статус

**Компоненти екрану статистики:**
- Область тексту для відображення статистики
- Кнопка завантаження з сервера
- Форматування: телеметрія, сон, тренування

**Компоненти екрану датчиків:**
- Статус симуляції (працює/зупинено)
- Кнопка запуску симуляції
- Кнопка зупинки симуляції
- Лог відправки даних (ScrolledText)

**Функціональність GUI:**
- Оновлення часу в реальному часі
- Завантаження даних з сервера по кнопці
- Запуск/зупинка симуляції в окремому потоці
- Логування дій симуляції
- Обробка помилок (відображення повідомлень)

#### 6.2. Main Application (`main.py`)

**Логіка запуску:**
1. Завантажити конфігурацію
2. Створити API клієнт
3. Створити симулятор датчиків
4. Створити та запустити GUI
5. Запустити головний цикл GUI (`root.mainloop()`)

**Інтеграція:**
- API клієнт передається в GUI
- Симулятор передається в GUI
- GUI керує симуляцією та відображенням

---

### **Година 7: Інтеграція та тестування (60 хв)**

#### 7.1. Інтеграція модулів

**Завдання:**
1. Перевірити зв'язок між модулями
2. Перевірити передачу даних між компонентами
3. Налаштувати багатопоточність (GUI + симуляція)

#### 7.2. Тестування з сервером

**Чек-лист тестування:**

**Відправка даних:**
- [ ] Відправка телеметрії (пульс) - перевірити в Swagger
- [ ] Відправка телеметрії (кроки) - перевірити в Swagger
- [ ] Відправка батчу телеметрії
- [ ] Відправка даних про сон
- [ ] Відправка даних про тренування
- [ ] Обробка помилок (недоступний сервер)
- [ ] Retry логіка (3 спроби)

**Отримання даних:**
- [ ] Отримання списку планів дієти
- [ ] Отримання деталей плану з прийомами їжі
- [ ] Отримання рекомендацій
- [ ] Отримання статистики за день
- [ ] Отримання статистики за тиждень
- [ ] Відображення даних в GUI

**GUI:**
- [ ] Відображення часу (оновлення)
- [ ] Відображення даних з датчиків
- [ ] Завантаження та відображення плану дієти
- [ ] Завантаження та відображення рекомендацій
- [ ] Завантаження та відображення статистики
- [ ] Запуск/зупинка симуляції
- [ ] Логування дій

#### 7.3. Обробка помилок

**Сценарії:**
- Сервер недоступний → показати повідомлення в GUI
- Невалідний device_id → логувати помилку
- Помилка мережі → retry з затримкою
- Помилка парсингу JSON → обробити виняток

---

### **Година 8: Документація та GitHub (60 хв)**

#### 8.1. README.md

**Структура:**
1. Опис проекту
2. Встановлення (крок за кроком)
3. Налаштування (конфігурація)
4. Запуск
5. Використання (як користуватися GUI)
6. API ендпоінти (які використовуються)
7. UML діаграми (посилання на файли)
8. Скріншоти GUI (опціонально)

#### 8.2. Коментарі в коді

**Що коментувати:**
- Кожен клас (призначення)
- Кожен метод (що робить, параметри, повертає)
- Складні алгоритми
- Важливі моменти інтеграції

#### 8.3. GitHub

**Структура репозиторію:**
- Основна гілка `main`
- `.gitignore` (venv, __pycache__, logs, config.json)
- Описований commit message

**Git команди:**
```bash
git init
git add .
git commit -m "Initial commit: IoT client with GUI (Apple Watch simulator)"
git branch -M main
git remote add origin <your-repo-url>
git push -u origin main
```

---

## 🔗 Інтеграція з сервером

### **API ендпоінти для використання:**

**Відправка даних (POST):**
- `POST /api/telemetry/receive` - телеметрія
- `POST /api/telemetry/receive/batch` - батч телеметрії
- `POST /api/sleep-records` - дані про сон
- `POST /api/training-sessions` - дані про тренування

**Отримання даних (GET):**
- `GET /api/dailydietplans?userId={id}` - список планів
- `GET /api/dailydietplans/{id}/meals` - деталі плану з прийомами
- `GET /api/recommendations/corrections?userId={id}` - рекомендації
- `GET /api/statistics/daily/{date}?userId={id}` - статистика за день
- `GET /api/statistics/weekly/{startDate}?userId={id}` - статистика за тиждень

### **Структури даних:**

**TelemetryReceiveDto:**
- `deviceId` (int)
- `timestamp` (ISO datetime)
- `telemetryType` (0=HeartRate, 1=Steps, 2=BloodPressure)
- `value` (decimal)
- `metadata` (optional dict)

**SleepRecordCreateDto:**
- `deviceId`, `date`, `totalSleepMinutes`, `deepSleepMinutes`, `lightSleepMinutes`, `awakeMinutes`, `sleepQuality`, `startTime`, `endTime`

**TrainingSessionCreateDto:**
- `deviceId`, `startTime`, `endTime`, `type`, `intensity`, `durationInMin`, `caloriesEstimated`

---

## 🚀 Як запустити все разом

### **Крок 1: Запустити .NET сервер**
```bash
cd FitnessProject
dotnet run
# Сервер працює на http://localhost:5006
```

### **Крок 2: Запустити Python IoT клієнт**
```bash
cd iot-client
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python src/main.py
```

### **Крок 3: Перевірити роботу**
1. Відкрити GUI додаток
2. Перейти на вкладку "Датчики"
3. Натиснути "Запустити симуляцію"
4. Перевірити в Swagger, що дані надходять на сервер
5. Перейти на вкладку "Дієта"
6. Натиснути "Завантажити план" (спочатку згенеруйте план на сервері)
7. Перевірити відображення даних

---

## ✅ Критерії готовності

- [ ] Код працює та відправляє дані на сервер
- [ ] GUI відображає дані з сервера
- [ ] Симуляція датчиків працює в окремому потоці
- [ ] UML діаграми створені
- [ ] README.md написаний
- [ ] Код завантажено в GitHub
- [ ] Тести пройдені успішно
- [ ] Конфігурація працює
- [ ] Обробка помилок реалізована

---

## 📝 Додаткові рекомендації

### **Для швидкої реалізації:**
1. Почніть з простого API клієнта (без retry)
2. Створіть мінімальний GUI (1-2 вкладки)
3. Симуляцію зробіть простою (тільки пульс і кроки)
4. UML діаграми створюйте паралельно з кодом
5. Тестуйте на кожному етапі

### **Якщо залишиться час:**
- Додати чергу для збереження даних при відсутності інтернету
- Додати графіки статистики (matplotlib)
- Реалізувати автоматичне оновлення даних (кожні 30 секунд)
- Додати звукові сповіщення для рекомендацій

---

**Успіхів! 🎉**
